{{>layouts/header}}
<div class="container mt-5 d-flex justify-content-center">
    <div class="col-12" style="max-width: 700px;">
        <div>
            <p class="text-center">
                {{username}},
                {{nickname}}
            </p>
        </div>
    </div>
</div>
{{>layouts/footer}}